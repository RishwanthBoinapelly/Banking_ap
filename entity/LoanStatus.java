package com.example.demo.entity;

public enum LoanStatus {
    PENDING,    // Loan is pending approval
    APPROVED,   // Loan is approved
    REJECTED,   // Loan is rejected
      // Loan is cancelled
}
